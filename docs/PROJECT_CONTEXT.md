# 项目背景

LiteTest-MAS 研究低开销多智能体测试生成。系统协调规划、检索、生成、执行和总结等逻辑角色，比较 Text、Protocol、StateVector 与 SharedMemory，同时保持 private official tests 不进入 Agent。

MBPP-Sanitized 是主要开发数据集，HumanEval+ 用于外部验证。公开任务保留原始数据集和任务 ID；参考实现、官方测试和私有评测材料不提供给 Agent。固定任务、固定模型实验的结论不外推为一般能力。
