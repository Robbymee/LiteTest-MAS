# 实验复现说明

## M9

M9 的有效 freeze 为 `cc7aac0417afb6acab47baaf7449459692fa9444`，规模为 240 条 final records。公开聚合位于 `reports/m9/`，其中 `analysis_manifest.json` 的 Strict Verifier 为 `valid=true`。复现公开分析不需要模型调用：

```bash
python -m pytest tests/test_m9_aggregation.py tests/test_m9_seed_sensitivity.py -q
```

## M9.1

M9.1 的有效 freeze 为 `c79fd4826627bf61faf5d90540a014d243a59edd`，Spec SHA 为 `3ad520c75bb66e8a4617daa64d6824183cbeaa5a1e1cb01dcd50035f145231f6`，公开聚合位于 `reports/m9_1/`。可执行：

```bash
python -m pytest tests/test_m9_1_aggregation.py tests/test_m9_1_verifier.py -q
python -m scripts.aggregate_m9_1_results --help
```

## 限制

公开复现只能读取公开聚合和公开 final records，不能读取 private tests 或重放模型响应。固定任务、固定模型、temperature=0 的统计结论不推广到其他模型、任务或配置。
